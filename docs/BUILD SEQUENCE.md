Recommended build sequence (do this in CoPilot)

Wire evaluator into web UI results page

Add AST scan to ExpoScanner and confirm it triggers:

location_background when startLocationUpdatesAsync found

Build Assets screen:

upload screenshots

run normalizeScreenshotsToPlay()

show download/export results

Export ZIP (next step after assets)